import { LoadingIcon } from '../icons/loader'

import { LoaderContainer } from './styled/loader.styled'

const Loader = () => (
  <LoaderContainer>
    <LoadingIcon />
  </LoaderContainer>
)

export default Loader
